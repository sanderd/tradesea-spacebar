{
  "name": "tradesea-spacebar",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "build:dev": "rollup -c --environment BUILD:dev"
  },
  "devDependencies": {
    "rollup": "^4.40.0",
    "@rollup/plugin-replace": "^6.0.0"
  }
}
